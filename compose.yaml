

services:
  app1:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./data:/app/data
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
    restart: "on-failure"

  # app2:
  #   build: .
  #   ports:
  #     - "8002:8000"
  #   volumes:
  #     - ./data:/app/data
  #   command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
  #   restart: "on-failure"

volumes:
  data:
